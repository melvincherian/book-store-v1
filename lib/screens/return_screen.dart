// ignore_for_file: file_names

import 'package:flutter/material.dart';

class ScreenReturn extends StatelessWidget {
  const ScreenReturn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}